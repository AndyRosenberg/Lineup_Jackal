<div class="jumbotron mt-1">
  <h1>Own your leage all week, every week.</h1>
  <p>The jackal interfaces with real-time fantasy football data. Create lineups from your team, compare potential outcomes with different starters, free agents, or matchups with opponents.</p>
  <p>
    <% if logged_in? %>
        <%= link_to "Create Lineup", new_lineup_path, class: "btn btn-primary btn-lg" %>
        <% if current_user.lineups.first %>
          <%= link_to "Compare", compare_lineup_path(current_user.lineups.last), class: "btn btn-default btn-lg" %>
        <% else %>
          <%= link_to "Compare", '', class: "btn btn-default btn-lg" %>
        <% end %>
    <% else %>
        <%= link_to "Sign Up", new_user_path, class: "btn btn-primary btn-lg" %>
        <%= link_to "Log In", login_path, class: "btn btn-default btn-lg" %>
      <% end %>
  </p>
</div>
<div>
  <%= image_tag("jackal.jpg", alt:"Responsive image", id: "home-logo" ) %>
</div>